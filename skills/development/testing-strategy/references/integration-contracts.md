# Integration and Contract Testing

For APIs/webhooks test schema-valid success, malformed/unauthorized requests, duplicate/replay, timeout/retry, pagination, version changes and out-of-order events. For databases test real constraints, migrations and transaction behavior. UI/E2E tests should assert user-observable outcomes rather than fragile implementation details. Flaky tests are operational defects; quarantine only temporarily with ownership and visibility.
