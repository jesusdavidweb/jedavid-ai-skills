# Incident Lifecycle

Detect/declare → scope impact → preserve evidence → mitigate → verify recovery → determine root cause → follow up. Distinguish trigger from contributing conditions. During an incident avoid unrelated refactors or cleanup. Recovery must be verified through external/user-facing health plus relevant error/latency signals.
